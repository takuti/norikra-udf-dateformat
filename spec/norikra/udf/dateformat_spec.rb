require 'spec_helper'

describe Norikra::Udf::Dateformat do
  it 'has a version number' do
    expect(Norikra::Udf::Dateformat::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
